# Add project specific ProGuard rules here.
# Room, Hilt, and kotlinx.coroutines ship consumer rules; this file is for
# app-specific exceptions only (kept intentionally short until v0.3.5 wires a
# real network/DTO layer that may need reflection-based rules).

-keepattributes *Annotation*
-keepattributes Signature
-keepattributes SourceFile,LineNumberTable
