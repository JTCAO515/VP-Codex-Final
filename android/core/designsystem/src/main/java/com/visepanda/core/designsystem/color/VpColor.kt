package com.visepanda.core.designsystem.color

import androidx.compose.ui.graphics.Color

// ── 底色 Surface ──
val SurfaceDarkest = Color(0xFF0A0A0A)       // 最深底（Hero/大区背景）
val SurfaceDefault = Color(0xFF1A1A1A)       // 标准表面（页面背景）
val SurfaceElevated = Color(0xFF232323)      // 卡片/容器
val SurfaceInteractive = Color(0xFF2A2A2A)   // 交互态（hover/选中/用户消息）

// ── 主金色 Accent ──
val GoldPrimary = Color(0xFFC9A96E)           // 主金色（品牌点缀/CTA/高亮）
val GoldDark = Color(0xFFB89255)              // 金色暗态（depressed/disabled）
val GoldLight = Color(0xFFDCC798)             // 金色亮态（hover/active glow）

// ── 辅助色 Secondary ──
val BambooGreen = Color(0xFF5B7B5A)           // 竹青（自然/成功/健康强调）
val JadeGrey = Color(0xFF8B8B7A)              // 玉石灰（辅助文本）
val JadeGreyDark = Color(0xFF6B6B5E)          // 更深的玉石灰（metatext）

// ── 中性色 Neutral ──
val TextPrimary = Color(0xFFF5F0E8)           // 米白（浅色文字/大标题）
val TextSecondary = Color(0xFFD4CEC4)         // 暖灰（正文/次要文字）
val TextMuted = Color(0xFF9C9A94)             // 暗灰色（placeholder）
val Divider = Color(0xFF3A3A3A)               // 分割线

// ── 功能色 Functional ──
val ErrorRed = Color(0xFFE55959)
val SuccessGreen = Color(0xFF5B9A5B)
